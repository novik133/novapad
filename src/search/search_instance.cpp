/**
 * @file search_instance.cpp
 * @brief Implementation file for SearchInstance (currently header-only).
 *
 * This file exists to satisfy the CMake source list.  All SearchInstance
 * logic is defined inline in the header via C++20 defaulted <=> operator.
 */

#include "search_instance.h"

// Intentionally empty — SearchInstance is a value type defined entirely
// in the header.  This .cpp is included for build-system consistency.
